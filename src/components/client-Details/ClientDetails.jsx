import React from 'react';

const ClientDetails = (props) => {
    return (
        <div>
            <p>Information about client: {props.Client}</p>
        </div>
    );
};

export default ClientDetails;